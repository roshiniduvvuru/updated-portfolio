services:
  - type: static_site
    name: roshini-portfolio
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: "20"
    routes:
      - path: /*
        destination: /index.html
